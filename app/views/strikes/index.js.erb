function refreshIndexCards(innerHTML) {
  const indexCards = document.getElementById('strikes-container');
  indexCards.innerHTML = innerHTML;
}

refreshIndexCards(
  '<%= j render "strikes/filtered_list", strikes: @grouped_strikes %>'
);
