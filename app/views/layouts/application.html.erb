<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Alerta Greves</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <div class="nav-top">
      <%= render 'shared/navbar' %>
    </div>
    <%= render 'shared/flashes' %>
    <div class="main-display">
    <div class="menu">
      <ul class="navbar-nav mr-auto">
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to "#{t(:login)}", new_user_session_path %>
          </li>
          <li class="nav-item">
            <%= link_to "#{t(:sign_up)}", new_user_registration_path %>
          </li>
        <% end %>

        <% if user_signed_in? %>

          <% unless current_user.admin %>
            <li class="nav-item">
              <%= link_to "#{t(:submit_strike)}", new_submitted_strike_path %>
            </li>
          <% end %>

          <% if current_user.admin %>
            <li class="nav-item">
              <%= link_to "#{t(:add_strike)}", new_strike_path %>
            </li>
            <li class="nav-item">
              <%= link_to "#{t(:approve_strikes)}", submitted_strikes_path %>
            </li>
          <% end %>

          <li class="nav-item">
              <%= link_to "#{t(:reminders)}", reminders_path %>
          </li>
          <li class="nav-item">
              <%= link_to "#{t(:settings)}", edit_user_registration_path %>
          </li>
          <li class="nav-item">
              <%= link_to "#{t(:about_us)}", root_path %>
          </li>
          <li class="nav-item">
            <%= link_to "#{t(:logout)}", destroy_user_session_path, method: :delete %>
          </li>
        <% end %>
    </div>
    <div>
    <%= yield %>
    </div>
    <div class="filter">WIP - FILTER OPTIONS HERE?</div>
    </div>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
